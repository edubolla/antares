---
import CtaLink from "./CtaLink.astro";
import SectionHeader from "./SectionHeader.astro";
import { images, testimonials } from "../data/landing";
---

<section id="depoimentos" class="section-padding bg-white">
  <div class="container-page">
    <div class="grid gap-12 lg:grid-cols-[0.9fr_1.1fr]">
      <div>
        <SectionHeader
          eyebrow="Depoimentos"
          title="Quem construiu com a Antares destaca segurança, atendimento e entrega."
          description="Depoimentos reais de clientes que confiaram à Antares o projeto, a execução ou a construção completa da casa."
        />
        <img
          src={images.testimonial}
          alt="Casa entregue usada como prova social"
          class="mt-8 aspect-[4/3] w-full rounded-[2rem] object-cover shadow-premium"
          loading="lazy"
        />
      </div>

      <div class="grid gap-4">
        {testimonials.map((testimonial) => (
          <blockquote class="rounded-[2rem] border border-antares-line bg-antares-soft p-6">
            <p class="text-lg leading-8 text-antares-ink">"{testimonial.quote}"</p>
            <footer class="mt-5 font-semibold text-antares-navy">{testimonial.name}</footer>
          </blockquote>
        ))}
        <CtaLink label="Quero conversar com a equipe" class="mt-2" />
      </div>
    </div>
  </div>
</section>
